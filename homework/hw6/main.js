const aElm = document.getElementById('a');
const bElm = document.getElementById('b');
const calculateBtn = document.getElementById('calculate');
calculateBtn.addEventListener("click", equation);

function equation() {
    let x = -(b) / a;
    alert('x = ' + x)
}