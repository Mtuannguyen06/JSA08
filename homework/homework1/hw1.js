const basket = [["grape", 15], ["apple", 12], ["orange", 10]];
for(let x = 0; x < basket.length; x++) {
    for(let y = 1; y >= 0; y--) {
        console.log(`i have ${basket[x][y]} ${basket[x][y]}`)

    }
}