1.biến
	const/let + tên biến = giá trị
	const là biến không thể thay đổi giá trị, let thì có

2.kiểu dữ liệu
	.string(chuỗi)	
		const = "abc"
	.number
		const x = 1
		const y = 3
	.booleans
		const x = true
		const y = false
	.array
	.object

3.câu điều kiện(if/else)
	const diem = 9
	if (diem > 8) {
	console.log('hoc sinh gioi')
	} else {
    	console.log('hoc sinh kha')
	}

